<?php

return [
    /*
    |--------------------------------------------------------------------------
    | セキュリティ設定
    |--------------------------------------------------------------------------
    |
    | HTTPクライアント、ファイルパス、ドメインホワイトリストなどの
    | セキュリティ関連の設定を行います。
    |
    */

    /*
    |--------------------------------------------------------------------------
    | 許可されたドメイン（ホワイトリスト）
    |--------------------------------------------------------------------------
    |
    | HTTPクライアントでアクセスを許可するドメインのリストです。
    | ここに追加されたドメインのみ外部APIへのアクセスが許可されます。
    |
    */
    'allowed_domains' => [
        // デフォルトのドメインは SecureHttpClientService で定義
        // ここに追加のドメインを設定できます
    ],

    /*
    |--------------------------------------------------------------------------
    | HTTPクライアント設定
    |--------------------------------------------------------------------------
    */
    'http_client' => [
        // デフォルトのタイムアウト（秒）
        'timeout' => env('HTTP_CLIENT_TIMEOUT', 10),

        // 最大リダイレクト回数（0でリダイレクト禁止）
        'max_redirects' => env('HTTP_CLIENT_MAX_REDIRECTS', 0),

        // 最大レスポンスサイズ（バイト）
        'max_response_size' => env('HTTP_CLIENT_MAX_RESPONSE_SIZE', 10485760), // 10MB
    ],

    /*
    |--------------------------------------------------------------------------
    | ファイルパス設定
    |--------------------------------------------------------------------------
    */
    'file_path' => [
        // 許可されたルートディレクトリ
        'allowed_roots' => [
            'storage' => 'storage/app/public',
            'public' => 'public',
        ],
    ],
];
